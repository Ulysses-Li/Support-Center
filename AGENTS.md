# Support Center 專案規範

本檔案記錄本專案的視覺、響應式設計、共用元件與 Git 維護方式。後續修改頁面時，請優先遵守這些規則，讓全站保持一致。

## 安全規則

- 禁止批量刪除文件或目錄。
- 不要使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
- 需要刪除文件時，只能一次刪除一個已確認的明確路徑文件。
- 若需要批量刪除，停止操作並請使用者手動處理。
- 保留使用者現有的未提交修改，不要把無關修改混入目前工作。

## 全站共用結構

- 所有頁面共用專案根目錄的 `header/`、`footer/`、`assets/`、`favicon.png` 與 `apple-touch-icon.png`。
- Header 和 Footer 必須維持全站一致，不在子資料夾建立重複版本。
- 共用背景優先使用根目錄 `css/site-background.css`，不要在各頁複製相同背景程式。
- 頁面自己的內容樣式放在對應功能資料夾，避免修改共用樣式影響其他頁面。

## Nine9 視覺風格

- 主色使用 Nine9 橘色 `#f05a1a`，輔色使用技術藍 `#0067ad`，主要文字使用深藍黑 `#0c2237`。
- 頁面背景使用明亮的淡藍技術網格，避免大面積深灰色內容區。
- 內容表面以白色為主，搭配淡藍圖示框、柔和陰影與適度圓角；避免在同一卡片重複使用外框、內框與分隔線。
- 橘色負責主要操作與品牌提示；藍色負責分類、編號及技術資訊，不要讓兩種顏色互相搶焦點。
- 字體維持清楚、粗實、容易掃讀；一般操作文字不得小於 `14px`，主要內容建議至少 `16px`。
- 圖示優先使用 Bootstrap Icons 官方 SVG 直接內嵌，避免依賴外部圖示服務造成搬站後失效。

## 產品目錄卡片

- 桌面版使用三欄白底技術型卡片，完整顯示分類圖、分類編號、名稱、相關產品與底部操作按鈕。
- 卡片頂端使用橘、藍雙色細線，不使用大片灰色標題背景。
- 分類圖片放在淡藍色獨立圖示框中，與名稱保持清楚間距。
- 相關產品使用留白與向右箭頭建立層級，每一列都要有足夠的點擊高度；手機版不重複堆疊橫向分隔線。
- 主要分類按鈕固定在卡片底部，讓同一列卡片的操作位置一致。
- Hover 只使用輕微上移、邊框與陰影變化，不使用過度動畫。

## 手機版產品卡片

- `575.98px` 以下改為單欄手風琴卡片，桌面與手機共用同一份產品資料及 HTML 結構。
- 手機卡片以左側橘線、淡藍圖示框、分類編號、名稱與圓形向下箭頭構成。
- 手機版預設全部收合；使用者點擊後才展開，開啟另一張時自動收合前一張，避免頁面過長。
- 展開箭頭使用 Bootstrap Icons `chevron-down`，展開後旋轉 `180deg` 成為向上箭頭。
- 按鈕必須同步更新 `aria-expanded` 與 `aria-controls`，保留鍵盤及輔助技術可用性。
- 手機點擊區至少約 `44px` 高，分類標題不可因過小字級而難以閱讀。

## 程式與註解

- HTML、CSS、JavaScript 的重要區段與非直觀邏輯要加入繁體中文註解，說明用途及後續調整位置。
- 產品名稱、圖片、連結等資料集中管理，避免在多個頁面重複手動維護。
- 動態輸出的文字必須先做 HTML 跳脫，避免資料被瀏覽器當成標記執行。
- 響應式設計以共用結構配合 CSS 切換呈現，不維護兩份互相分離的桌面與手機頁面。
- 動畫需要支援 `prefers-reduced-motion`，尊重使用者的減少動畫設定。

## 樣板確認流程

- 未確認的新設計先放在根目錄 `測試資料/` 下的獨立資料夾。
- 樣板應使用實際產品名稱與現有本機圖片，確保評估結果接近正式頁面。
- 使用者確認樣板後，再將結構與樣式整理套用到正式頁，不能直接讓正式頁引用測試資料。
- 套用正式頁後，要檢查桌面與手機操作、資源路徑、JavaScript 語法及現有測試。

## Git 與發布

- 每完成一個可清楚說明的功能段落，就建立一筆 Git commit。
- Commit 訊息使用繁體中文，例如：`套用響應式產品卡片設計`。
- 每筆 commit 只包含該段落相關檔案，不混入其他未提交修改。
- 發布前先完成基本驗證，確認工作目錄沒有意外檔案，再推送至 GitHub。
- 若使用者只要求本機樣板，先不要發布；正式頁修改完成並確認後再發布。
